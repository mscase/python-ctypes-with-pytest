working with ctypes and pytest

wrap some of the features from libtiff with ctypes and exercise them with python

python sample.py

python -m pytest -v
python -m pytest -m ctiff
python -m pytest -m easytif

